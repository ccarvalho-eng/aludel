defmodule Aludel do
  @moduledoc """
  Phoenix-native LLM evaluation workbench.

  Aludel provides versioned prompts, multi-provider runs, reusable datasets,
  evaluation suites, structured assertions, document inputs, prompt evolution
  analytics, exports, and headless CI execution. It can be embedded in a host
  Phoenix application or run as a standalone dashboard.

  Start with the [Feature Guide](features.html), [Evaluation Guide](evaluations.html),
  or [Embedding Guide](embedding.html).
  """
end
